- hosts: all
  vars:
    test: "Test Successfull"
  tasks:
     - debug:
          msg: "{{ test.split()[0] }} {{ test.split()[1] }}"
          

          #  - name: Running npm install in other directories
          # command: npm install
          #when: flag | default (false)
          #args:
          # chdir: "{{ item }}"
          #loop:
          #- /media/disk1/sandbox/connecto/ui 
         
          # - /media/disk1/sandbox/connecto/api

